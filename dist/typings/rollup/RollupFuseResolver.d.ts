import { WorkFlowContext } from "../core/WorkflowContext";
export declare function RollupFuseResolver(context: WorkFlowContext, root: string): {
    resolveId(importee: any, importer: any): string;
};
